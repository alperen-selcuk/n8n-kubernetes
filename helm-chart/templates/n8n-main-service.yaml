apiVersion: v1
kind: Service
metadata:
  name: n8n-main
  namespace: {{ .Values.namespace }}
  labels:
    app: n8n-main
spec:
  clusterIP: None
  selector:
    app: n8n-main
  ports:
  - name: http
    port: {{ .Values.service.port }}
    targetPort: {{ .Values.service.targetPort }}
